package mainGameCode;

public class Stats {

	/* TODO: Create variables for all stats that are in the Player class.
	 * 		 (We will exclude health, mana, xp, name, damage, defense, and level stats, and their max variables as well)
	 * 
	 * TODO: Create getters/setters for each of the variables
	 * 		 (i.e. public void getStrength() {return strength;}
	 * 		 public int setStrength(int strength) {this.strength = strength;})
	 * 
	 * TODO: In the player class, implement this stats class.
	 * 		 i.e. Stats stats;
	 * 		 and in the constructor, do: stats = new Stats();
	 * 
	 * TODO: The constructor of this class can be the setting of the base stats.
	 * 		 Create an extra variable for each stat called base<statHere> and set those
	 * 		 in the constructor.
	 * 
	 * 
	 */
	
}
